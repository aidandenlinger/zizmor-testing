name: Test action

on:
  workflow_dispatch:
    sample_input:
      description: Will be echoed
      type: string
      required: true


jobs:
  testing-stuff:
    runs-on: ubuntu-latest

    steps:
      - name: Echo the input directly
        run: echo ${{ inputs.sample_input }}

